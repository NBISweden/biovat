Changes in component 'nf-core/parabricks/fq2bam'
'modules/nf-core/parabricks/fq2bam/meta.yml' is unchanged
Changes in 'parabricks/fq2bam/main.nf':
--- modules/nf-core/parabricks/fq2bam/main.nf
+++ modules/nf-core/parabricks/fq2bam/main.nf
@@ -37,7 +37,9 @@
     def args = task.ext.args ?: ''
     def prefix = task.ext.prefix ?: "${meta.id}"
 
-    def in_fq_command = meta.single_end ? "--in-se-fq ${reads}" : "--in-fq ${reads}"
+    def readgroup_tags = "\"@RG\\tID:${meta.read_group}\\tSM:${meta.id}\\tLB:${meta.library}\\tPL:${meta.pl}\""
+
+    def in_fq_command = meta.single_end ? "--in-se-fq ${reads} ${readgroup_tags}" : "--in-fq ${reads} ${readgroup_tags}"
     def extension = "${output_fmt}"
 
     def known_sites_command = known_sites ? (known_sites instanceof List ? known_sites.collect { knownSite -> "--knownSites ${knownSite}" }.join(' ') : "--knownSites ${known_sites}") : ""

'modules/nf-core/parabricks/fq2bam/tests/main.nf.test.snap' is unchanged
'modules/nf-core/parabricks/fq2bam/tests/main.nf.test' is unchanged
'modules/nf-core/parabricks/fq2bam/tests/nextflow.config' is unchanged
************************************************************
